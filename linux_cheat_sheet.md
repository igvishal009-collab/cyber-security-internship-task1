# Essential Linux Commands Cheat-Sheet

## Navigation & Operations
- `ls -la` : List all files with permissions
- `chmod 700 secret.txt` : Restrict file permissions
- `ifconfig` / `ip addr` : Check active network interfaces
- `netstat -rn` : View kernel routing table
